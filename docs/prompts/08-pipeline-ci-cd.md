# Prompt Vigente: CI/CD

Crie pipeline para a aplicacao Next.js ativa com estes passos:

- instalar dependencias em `frontend/`;
- executar `npm test`;
- executar `npm run build`.

O workflow deve disparar em `push` e `pull_request` para a branch principal do repositorio.
